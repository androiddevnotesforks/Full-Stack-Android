package com.nexters.fullstack.domain.source.local

import com.nexters.fullstack.domain.source.data.LocalImageDomain

data class DomainUserImage(val labels: List<DomainUserLabel>, val image: LocalImageDomain)
